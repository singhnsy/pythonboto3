import boto3

# Create an s3 client

s3 = boto3.client('s3')

# List all buckets
response = s3.list_buckets()
#print(response)
#print(response["Buckets"])
for i in response["Buckets"]:
    print(i["Name"])



=========
=========

import boto3

s3 = boto3.client('s3')

#Specify the file to upload

filename = 'boto3s3.txt'
bucket_name = 'deepakdevops12'
destfile = 'botonewfile'

s3.upload_file(filename, bucket_name, filename)
print("File has been uploaded into the s3 bucket")


=========
=========

import boto3
s3 = boto3.client('s3')

filename = 'boto3s3.txt'
bucket_name = 'deepakdevops12'
destfile = 'botonewfile'

s3.download_file(bucket_name, filename, destfile)
print("File downloaded")

=========
=========

import boto3

# Create a session using your credentials
s3 = boto3.client('s3')


# List all the buckets in your AWS account
response = s3.list_buckets()

#print(response)

#print(response['Buckets'])

for i in response['Buckets']:
    print(i["Name"])
 
#print("S3 Buckets:")
#for bucket in response['Buckets']:
 #   print(f'  {bucket["Name"]}')
 
========
========

import boto3

# Create a session using your credentials
s3 = boto3.client('s3')
response = s3.list_buckets()
#print(response)
#print(response["Buckets"])
for i in response["Buckets"]:
    print(i["Name"])
# List all the buckets in your AWS account

#print(response["Buckets"])
#for i in response["Buckets"]:
 #   print(i["Name"])

# for bucket in s3.buckets.all():
 #   print(bucket.name)


=======
=======

import boto3

# Create an s3 client

s3 = boto3.client('s3')

# List all buckets
response = s3.list_buckets()
#print(response)
#print(response["Buckets"])
for i in response["Buckets"]:
    print(i["Name"])


# Specify the file to upload
filename = 'test.txt'
bucket_name = 'deepakdevops12'

# Upload the file to S3
#s3.upload_file(filename, bucket_name, filename)
#print(f'{filename} uploaded to {bucket_name}')

# Download the file
s3.download_file(bucket_name, filename, 'downloaded_file.txt')
print(f'{filename} downloaded from {bucket_name}')
