# Get the authenticated user
user = g.get_user("Shikhar82")
# List repositories
for repo in user.get_repos():
    print(f"Repository Name: {repo.name}")
	
=======
========
	
#Example 3: Create a New Repository
from github import Github
# Replace 'your_access_token' with your actual GitHub token
token = "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"
g = Github(token)
# Get the authenticated user
user = g.get_user()
print(f"Username: {user.login}")
# Create a new repository
repo_name = "test-repo1"
repo = user.create_repo(repo_name)
print(f"Repository {repo_name} created successfully.")

======
======

from github import Github
# Replace 'your_access_token' with your actual GitHub token
token = "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"
g = Github(token)
# Get the authenticated user
user = g.get_user()

# Get a specific repository
repo = g.get_repo("Shikhar82/test-repo")

# Create an issue
issue = repo.create_issue(title="Test Issue", body="This is a test issue.")
print(f"Issue created: {issue.title}")

======
======